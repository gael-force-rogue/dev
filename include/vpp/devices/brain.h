#include "vpp/helpers.h"

namespace vpp {
    struct Dataset {
    };

    // class Brain {
    // private:
    //     vex::brain brain;
    //     bool userControl = true;

    // public:
    //     Brain();

    //     /**
    //      * @brief If the user is in control, print to the brain
    //      */
    //     void printf(int line, const char* format, ...) {
    //         if (!userControl) return;

    //         va_list args;
    //         va_start(args, format);
    //         brain.Screen.setCursor(line, 1);
    //         brain.Screen.clearLine();
    //         brain.Screen.print(format, args);
    //         va_end(args);
    //     };

    //     void graph(std::vector){

    //     };
    // };
}  // namespace vpp